version: 0.0
os: linux
files:
  - source: /
    destination: /var/www/html/  # Or any path where you want to place the files
hooks:
  AfterInstall:
    - location: scripts/after_install.sh
      timeout: 300
      runas: root
